# +---------------------------------+
# |           Section 29            |
# +---------------------------------+
# | Building a Password Manager GUI |
# |        App with Tkinter         |
# +---------------------------------+

# Use columnspan in grid() function to set how many columns a widget should span

# To make cursor automatically focus on a given widget, we can use the widget.focus() command

# The join() method takes all items in an iterable and joins them into one string
#   A string must be specified as the separator
#   Example: fruits = ", ".join(list_of_fruits)
#            fruits = "Apple, Orange, Pear, Pineapple, ..., etc."

# Pyperclip is a useful module for copy and paste clipboard functions
#   https://pypi.org/project/pyperclip/
#   pyperclip.copy('The text to be copied to the clipboard') <- Will copy text
#   pyperclip.paste() <- Will paste text
