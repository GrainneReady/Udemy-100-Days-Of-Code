# +-------------------------------+
# |          Section 20           |
# +-------------------------------+
# |    Building the Snake Game    |
# |    Animation & Coordinates    |
# +-------------------------------+

# Steps For Today
#   1. Create a Snake body
#   2. Move the snake
#   3. Control the snake

# for seg_num in range(len(self.segments) - 1, 0, -1):  # range(Start = len(self.segments) - 1, Stop = 0, Step = -1)
#   The range() function is derived from C but the start, stop and step aren't positional arguments in python. This is just to show what each parameter represents in this case.
