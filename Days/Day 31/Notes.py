# +--------------------+
# |     Section 31     |
# +--------------------+
# |  Capstone Project  |
# | Flash Card Program |
# +--------------------+

# orient = "records" in DataFrame.to_dict (pandas) sets the type of values
# of the new dictionary as a list like [{column -> value}, … , {column -> value}]

# window.after_cancel(flip_timer) cancels flip_timer from running

# You can edit canvas text by giving it a variable and editing it in canvas.itemconfig(variable, text="new text"), or any other attribute for that matter.
#   (works with images as well as many others)
